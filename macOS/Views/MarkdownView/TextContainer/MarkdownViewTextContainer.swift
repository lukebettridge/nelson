//
//  MarkdownViewTextContainer.swift
//  Nelson (macOS)
//
//  Created by Luke Bettridge on 24/02/2021.
//

import AppKit

class MarkdownViewTextContainer: NSTextContainer {
//    override func lineFragmentRect(forProposedRect proposedRect: CGRect, at characterIndex: Int, writingDirection baseWritingDirection: NSWritingDirection, remaining remainingRect: UnsafeMutablePointer<CGRect>?) -> CGRect {
//        let rect = super.lineFragmentRect(forProposedRect: proposedRect, at: characterIndex, writingDirection: baseWritingDirection, remaining: remainingRect)
//        
//        if let textView = textView, textView.isEditable && !textView.string.isEmpty {
//            
//            let character = characterFromUTF16CodeUnits(textView.string.utf16, at: characterIndex)
//            if character == "#" {
//                return rect.offsetBy(dx: 25, dy: 0)
//            }
//        }
//        
//        return rect.offsetBy(dx: 50, dy: 0)
//    }
}
